/* English strings. Empty by design in v3.0 — the lookup falls back to the key, so the app
   reads exactly as it did before. Keys are semantic (btn.launch_test), never the English
   text: if the key were the wording, changing the wording would break every translation. */
const LANG_EN = {
};
